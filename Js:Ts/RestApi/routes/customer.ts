import express, {Response, Request} from "express";
import { Customer } from "../typescript/interface.customer";

const router = express.Router();

router.get("/", (req: Request, res: Response) => {
  
});

export default router;