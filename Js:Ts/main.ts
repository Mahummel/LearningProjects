import { ArraysAndStrings } from "./class.arrays-strings";
const SectionOne = new ArraysAndStrings('');

import { LinkedList, Node } from "./class.linked-lists";
const SectionTwo = new LinkedList<number>(new Node(1));

import { SetOfStacks } from "./class.stack-queue";
const stack = new SetOfStacks<number>(2);

